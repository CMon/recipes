* look into the main.cpp's of the different products

* general ideas:
  * add a client that will have a console input with readline and a bash like completion, which uses the `discovery` call to fill the completion
